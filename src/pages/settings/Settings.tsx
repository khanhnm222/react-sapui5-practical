import { Title, Card } from '@ui5/webcomponents-react';

const Settings = () => {
  return (
    <Card header={<Title>Settings</Title>}>
      <p>Update application settings here.</p>
    </Card>
  );
};

export default Settings;
